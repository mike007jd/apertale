# Bug ledger

| ID | Severity | Finding | Resolution | Evidence |
|---|---|---|---|---|
| QL-01 | P0 | Preserved-photo readiness said ready while the old authoring contract still required regenerated interiors. | One versioned brief contract now gives preserved albums zero generated interiors, source-true layouts, and explicit crop/colour/identity policy across prompt, tools, scene policy, rubric, and Worker. | readiness tests; preserved Worker publish test |
| QL-02 | P0 | A brief could label itself illustrated and bypass missing/unknown personal assets or identity policy. | Readiness derives risk from actual `sourceAssets`; browser and Worker require verified ids, source use, identity preservation, and face changes disabled whenever personal photos exist. | source-bypass unit and forged-Worker tests |
| QL-03 | P1 | `quality-review` was advertised before the tool returned a real quality contract. | Detail now returns the versioned rubric, deterministic results, render manifest/evidence, stored brief, and round state. | WebMCP contract tests + live JSON |
| QL-04 | P1 | Tool descriptions did not teach readiness questions or the bounded critique loop. | The six-tool catalog now states readiness → questions → exact brief → render → begin/record critique → at most two patches → publish gate. | description tests + live catalog |
| QL-05 | P1 | Workshop promised a ready state it could never reach because premise/audience belong to the Agent conversation. | UI honestly says **Finish the brief in Codex**, displays concrete questions, and keeps Image handoff secondary. | `02-workshop-incomplete.png` |
| QL-06 | P0 | Follow-up review found Worker identity drift, overloaded composite/photo provenance, and no legacy-book adoption path. | Worker reuses source-based identity rules; `sourceAssetId` is the original composite and `personalSourceAssetId` is declared user-photo provenance; legacy personal books may adopt one readiness-passed brief at an exact revision. | 58/58 directed tests + independent PASS |
| QL-07 | P1 | Worker accepted arbitrary syntactically valid checked-in asset paths and incomplete visual evidence coverage. | Build generates/checks a 149-entry bundled asset catalog; Worker rejects unknown paths and requires current document spread coverage. | Worker/Sites tests |
| QL-08 | P1 | Live render duplicated two character cutouts over the same characters baked into the base. | Round-1 blockers forced a new ImageGen clean plate and safe-zone transform before the final round. | `03-story-render-before-critique.png`, `critique-round-1.json`, `07-story-render-after-patch.png` |
| QL-09 | P2 | Singular UI/tool copy rendered “1 spreads/photos”. | Count-aware copy now uses “1 spread/photo”. | UI screenshots + creation tests |
| QL-10 | P1 | Editing a report approved on round two could make `begin-critique` advertise an impossible round three. | A post-approval revision starts a fresh bounded two-round cycle; blocked cycles retain their consumed rounds and still stop for material/decision. | BookEngine cycle regression |
| QL-11 | P1 | New personal books had no legacy `textureUrl`, so forced 2D fallback could neither show final artwork nor record evidence; its failure state could also remain hidden under the loading overlay. | Fallback resolves the final clean plate and every non-procedural foreground, records only after all images load, and terminates loading with visible **Visual review unavailable** and no spread evidence on failure. | `09-fallback-render.jpg`, `10-fallback-unavailable.jpg`, fallback JSON, regression + production build |
| QL-12 | P1 | WebGL evidence could be stamped while foreground assets were pending/failed, and App discarded the captured revision. | ThreeBook now waits for the exact spread element/texture set with no pending/failure; App forwards the captured revision and Engine rejects stale evidence. | render-evidence + stale-revision regressions |
| QL-13 | P2 | Browser accepted a meaningful procedural hotspot while Worker required the interaction on a non-procedural foreground. | Both gates allow any authored element to provide the meaningful interaction while independently requiring 2–4 real foreground layers. | browser/Worker parity fixtures |
| QL-14 | P2 | `manage_book(open)` required `expectedRevision` in schema but ignored it at execution. | Open now rejects missing or stale revisions before changing the active document and preserves request-id idempotency. | WebMCP missing/stale/open regressions |
| QL-K01 | Known separate gate | 66 referenced legacy v2 cutouts fail the existing alpha/padding audit; 16 v3 assets pass. | Preserved as an explicit pre-existing asset-production gate. No mechanical padding or deletion was introduced in this feature branch. | existing `npm run audit:cutouts` result |
