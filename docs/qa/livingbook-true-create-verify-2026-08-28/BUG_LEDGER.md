# Apertale True Create Your Own — Bug Ledger

| ID | Severity | Surface | Confirmed behavior / evidence | Root cause | Fix / oracle | Status |
|---|---:|---|---|---|---|---|
| CYO-B01 | P0 | Photo-led creation | Prior live session placed each uploaded photo on a right page and called the flow complete; no ImageGen-created cover or spread art existed | Tests exercised Site Tools mutation, not semantic book quality; starter contract did not block layout-before-generation | Typed two-phase creation brief; required counts and fixed gates; reject right-page dump in prompt, skill, tool descriptions, and tests | fixed; local + production full-book reruns passed |
| CYO-B02 | P1 | Creation workshop | Previous workshop was text-heavy and required users to infer source mode and image order | Explanatory copy substituted for direct controls and visible asset state | Compact single-sheet UI; source segments; thumbnails; reorder/remove; short runtime status | fixed; desktop, side-pane, and mobile live checks passed |
| CYO-B03 | P1 | Narrow/mobile layout | Previous dialogs and reader surfaces were not consistently usable in the Codex side pane | Desktop-biased sizing and verbose stacked content | Responsive workshop layout and 44px interaction targets | fixed; 760×900 and 390×844 live checks passed |
| CYO-B04 | P1 | CTA/browser copy | Hero overexplained Safari/ChatGPT behavior and primary action was model-branded instead of task-branded | Browser explanation dominated the user goal | **Create your own** CTA; concise Codex (ChatGPT desktop) handoff; no Safari mention | fixed; production live check passed |
| CYO-B05 | P1 | Delegated integration | Opus workshop initially still emitted the old weak inline prompt after Cursor added the new contract | Parallel workstreams ended at adjacent modules without final wiring | `App.tsx` now calls `buildCreationBrief` directly; targeted regression test + typecheck + build | fixed and verified |
| CYO-B06 | P1 | Creation brief copy | Restricted webviews reported “Copied” while writing zero clipboard bytes | Optional clipboard call treated API absence as success | Clipboard write plus selection fallback; success state only after a true write; manual selectable brief on failure | fixed and live verified (4,358 bytes) |
| CYO-B07 | P2 | Workshop touch target | Workshop close target measured 42×42px in the side pane | Legacy fixed size missed the 44px floor | 44×44px target; live geometry assertion | fixed and live verified |
| CYO-B08 | P1 | Site Tool schema | `manage_book` executed create/set-cover only with `expectedRevision`, but its published schema did not require it | Conditional executor contract was not reflected in the shared schema | Publish `expectedRevision` as required and assert it in the WebMCP registration test | fixed; production discovery passed |
| CYO-B09 | P1 | Photo brief persistence | Opening a new workshop auto-selected the oldest 12 global IndexedDB assets, mixing prior projects into a fresh brief and hiding newer imports after reload | Global asset history was used as selection state | Persist only the current tab's explicitly selected ordered IDs; hydrate those exact IDs; keep unselected blobs discoverable to existing books/WebMCP | fixed; fresh/reload/remove live checks passed |
| CYO-B10 | P2 | Import error controls | A duplicate late CSS rule overrode the new import recovery target back to 30px | Stale workshop rule survived the UI replacement | Consolidated rule keeps the recovery control at 44px and removes the cascade collision | fixed; style audit and live geometry passed |
| CYO-B11 | P1 | Publish panel | Production publication panel still presented three long warning paragraphs after the creation workshop was simplified | Safety guidance was written as prose instead of scannable decisions | Three compact disclosure facts, short status line, 44px close target, and shorter delete confirmation | fixed; local live check, 56 tests, typecheck, and build passed |
